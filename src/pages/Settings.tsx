import { PageFrame } from "@/components/PageFrame";

export function Settings() {
  return <PageFrame eyebrow="Settings" title="Workspace controls" description="Add permissions, approval thresholds, reporting defaults, and integration settings here." />;
}
