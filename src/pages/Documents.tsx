import { PageFrame } from "@/components/PageFrame";

export function Documents() {
  return <PageFrame eyebrow="Documents" title="Document control" description="Track instructions, submissions, technical queries, and formal notices from a single register." />;
}
